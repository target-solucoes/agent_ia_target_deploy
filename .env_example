# ============================================================================
# CONFIGURACAO DOCKER - LEIA ISTO PRIMEIRO!
# ============================================================================
# Este arquivo e um TEMPLATE. Para usar o aplicativo, siga estes passos:
#
# PASSO 1: Copiar este arquivo
#   Windows PowerShell:  Copy-Item .env_example .env
#   Git Bash/Linux/Mac:  cp .env_example .env
#
# PASSO 2: Editar o arquivo .env criado
#   - Substitua "sk-proj-..." pela sua OPENAI_API_KEY real
#   - Obtenha em: https://platform.openai.com/api-keys
#
# PASSO 3: Executar via Docker
#   docker-compose up --build
#
# PASSO 4: Acessar aplicacao
#   http://localhost:8501
#
# IMPORTANTE:
#   - NUNCA commite o arquivo .env no git (ele esta no .gitignore)!
#   - O arquivo .env e montado no container via volume read-only
#   - Mudancas no .env aplicam-se apos: docker-compose restart
#
# Documentacao completa: README_DOCKER.md
# ============================================================================


# ============================================================================
# OpenAI API Configuration (OBRIGATORIO)
# ============================================================================
# Obtenha sua API key em: https://platform.openai.com/api-keys
# Formato: sk-proj-...
OPENAI_API_KEY=sk-proj-...


# ============================================================================
# LLM Performance Settings (Problema #3 - Centralized Optimizations)
# ============================================================================
# Todas as configurações abaixo são OPCIONAIS e têm defaults otimizados.
# As configurações centralizadas em src/shared_lib/core/config.py incluem:
#
# - timeout=30s (redução de 50% do padrão 60s)
# - max_retries=2 (fail fast ao invés de muitas tentativas)
# - max_tokens=1500 (otimizado, 2500 para formatter)
# - reasoning_effort específico por agente
#
# Você pode sobrescrever os defaults usando as variáveis abaixo:

# Modelo OpenAI (default: gpt-5-nano-2025-08-07)
# OPENAI_MODEL=gpt-5-nano-2025-08-07

# Máximo de tokens na resposta (default: 1500, formatter: 2500)
# MAX_TOKENS=1500

# Reasoning effort para gpt-5-nano (default varia por agente)
# Opções: minimal, low, medium, high
# - formatter: minimal (narrativas simples)
# - filter: minimal (parsing rápido)
# - graphic: low (classificação consistente)
# - insight: minimal (insights rápidos)
# REASONING_EFFORT=minimal


# ============================================================================
# Dataset Configuration
# ============================================================================
DATASET_PATH=data/datasets/DadosComercial_resumido_v02.parquet


# ============================================================================
# Filter Classifier Settings (Opcional)
# ============================================================================
# Timeout para sessão de filtros (em minutos)
# FILTER_SESSION_TIMEOUT=30

# Threshold para fuzzy matching de filtros
# FILTER_FUZZY_THRESHOLD=0.75

# Confiança mínima para aceitar filtro
# FILTER_MIN_CONFIDENCE=0.60


# ============================================================================
# Supabase Configuration
# ============================================================================
SUPABASE_URL=https://...
SUPABASE_API_KEY=adT...